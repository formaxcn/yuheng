version: '3'

services:
  yuheng:
    build: .
    image: yuheng:latest
    container_name: yuheng
    restart: unless-stopped
    ports:
      - "3000:3000"
    environment:
      - GEMINI_API_KEY=${GEMINI_API_KEY}
      - MODEL=${MODEL:-gemini-1.5-flash}
